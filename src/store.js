import { createStore } from "vuex";

export default createStore({
  state() {
    return {
      views: {
        cliente: {
          dialogForm: false,
        },
      },
    };
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
});
